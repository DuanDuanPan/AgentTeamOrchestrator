Conventions: snake_case for modules/functions/SQLite columns/config keys, PascalCase for classes and Pydantic models, strict mypy on public functions, no `print()` for logging (use structlog), no SQL string concatenation, prefer Pydantic `model_validate()`, and keep config/data models in the appropriate modules. After changes, run ruff, mypy, targeted pytest, then full pytest and pre-commit when relevant.